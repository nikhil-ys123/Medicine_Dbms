<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Medicine Management</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main>
    <h1>💊 Medicine Management</h1>
    
    <div style="margin-bottom: 24px; text-align: center;">
      <a href="/" style="color: var(--accent-cyan); font-weight: 500; text-decoration: none; font-size: 1.1rem;">
        ← Back to Dashboard
      </a>
    </div>

    <div class="box">
      <h2>All Medicines</h2>
      
      <% if (medicines.length === 0) { %>
        <p style="color: var(--text-secondary); text-align: center; padding: 40px; font-style: italic;">
          No medicines found. Add some medicines from the dashboard.
        </p>
      <% } else { %>
        <table class="data-table">
          <thead>
            <tr>
              <th>Medicine Name</th>
              <th>Quantity</th>
              <th>Price per Pack</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% medicines.forEach(m => { %>
              <tr>
                <td><%= m.med_name %></td>
                <td><%= m.quantity %></td>
                <td>₹<%= m.price_prpk %></td>
                <td class="actions-col">
                  <button class="edit-btn edit-medicine-btn" 
                          data-id="<%= m.med_id %>">
                    ✏️ Edit
                  </button>
                  <button class="delete-btn delete-medicine-btn" 
                          data-id="<%= m.med_id %>" 
                          data-name="<%= m.med_name %>">
                    🗑️ Delete
                  </button>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </main>

  <script src="/main.js"></script>
</body>
</html>