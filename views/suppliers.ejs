<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Supplier Management</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main>
    <h1>🏪 Supplier Management</h1>
    
    <div style="margin-bottom: 24px; text-align: center;">
      <a href="/" style="color: var(--accent-cyan); font-weight: 500; text-decoration: none; font-size: 1.1rem;">
        ← Back to Dashboard
      </a>
    </div>

    <div class="box">
      <h2>All Suppliers</h2>
      
      <% if (suppliers.length === 0) { %>
        <p style="color: var(--text-secondary); text-align: center; padding: 40px; font-style: italic;">
          No suppliers found. Add some suppliers from the dashboard.
        </p>
      <% } else { %>
        <table class="data-table">
          <thead>
            <tr>
              <th>Supplier Name</th>
              <th>Address</th>
              <th>Contact</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% suppliers.forEach(s => { %>
              <tr>
                <td><%= s.supp_name %></td>
                <td><%= s.supp_add %></td>
                <td><%= s.supp_contact %></td>
                <td class="actions-col">
                  <button class="edit-btn edit-supplier-btn" 
                          data-id="<%= s.supp_id %>">
                    ✏️ Edit
                  </button>
                  <button class="delete-btn delete-supplier-btn" 
                          data-id="<%= s.supp_id %>" 
                          data-name="<%= s.supp_name %>">
                    🗑️ Delete
                  </button>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </main>

  <script src="/main.js"></script>
</body>
</html>